---
lesson_id: CSK-DS-01-01
title: "배열과 연결 리스트는 무엇이 다른가"
module: CSK-DS-01
level: foundation
subject: data-structures
language: ko
est_minutes: 20
tags: ["data-structures", "array", "linked-list", "complexity"]
---

## 1) 학습 목표

- 배열과 연결 리스트의 기본 구조를 설명한다
- 접근과 삽입의 차이를 판단한다
- 연속된 메모리 배치의 의미를 이해한다

## 2) 왜 중요한가

자료구조의 차이는 알고리즘 이전에 성능과 구현 난이도에 직접 연결된다. 배열과 연결 리스트는 그 대표적인 대비다.

## 3) 핵심 설명

- 배열은 연속된 위치에 데이터를 두기 때문에 인덱스로 빠르게 참조하기 쉽다
- 연결 리스트는 요소를 참조로 연결해 중간 삽입 구조를 설명하기 쉽다
- 다만 연결 리스트는 목적 위치까지 따라가는 비용이 든다

## 4) 작동 방식 또는 판단 흐름

1. 문제에서 임의 접근이 자주 필요한지 본다
2. 중간 삽입과 삭제가 많은지 본다
3. 메모리 지역성과 구현 단순성 중 무엇을 우선할지 결정한다

## 5) 비슷한 개념과의 비교

- 배열은 위치 기반이고 연결 리스트는 참조 기반 구조다
- 이론상의 삽입 비용뿐 아니라 위치 탐색 비용까지 포함해 판단해야 한다

## 6) 자주 하는 오해

- 연결 리스트면 항상 삽입이 빠르다는 오해
- 배열은 크기 변경이 전혀 안 된다는 오해
- 시간 복잡도만 보면 실무에서도 항상 맞는 선택이라는 오해

## 7) 확인 질문

1. 인덱스 접근이 중요한 경우 어느 쪽이 더 적합한가
2. 중간 추가가 많다면 무엇을 먼저 확인해야 하는가
3. 연속 메모리 배치는 캐시 관점에서 어떤 의미를 가지는가
4. 이론과 실무의 성능 차이가 생기는 이유는 무엇인가

## 8) 문항 추출 메모

- 배열과 연결 리스트의 접근 대비를 비교형 문항으로 추출할 수 있다
- 메모리 배치와 캐시를 연결한 해석형 문항 후보가 된다
- 문제 상황에 맞는 구조 선택 문제를 만들 수 있다
