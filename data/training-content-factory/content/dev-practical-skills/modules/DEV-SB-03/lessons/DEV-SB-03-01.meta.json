{
  "lesson_id": "DEV-SB-03-01",
  "category": "dev-practical-skills",
  "subject": "spring-boot",
  "topic": "configuration-profiles",
  "summary": "Covers Spring Boot configuration files (application.yml/properties), profile-based environment separation (dev/staging/prod), @Value vs @ConfigurationProperties, and configuration precedence rules.",
  "key_points": [
    "Profile-specific files (application-{profile}.yml) override base application.yml for environment separation.",
    "@ConfigurationProperties is preferred over @Value for type-safe, grouped configuration binding.",
    "Configuration precedence: command-line args > environment variables > profile YAML > base YAML."
  ],
  "misconceptions": [
    "application.yml is completely ignored when a profile-specific file exists.",
    "File-based configuration takes precedence over environment variables.",
    "Mixing YAML and Properties formats in the same project causes no issues."
  ],
  "check_questions": [
    "What is the relationship between application.yml and application-prod.yml?",
    "When should you use @ConfigurationProperties instead of @Value?",
    "How should production secrets like database passwords be managed?"
  ],
  "exam_extractables": {
    "fact_candidates": [
      "Spring Boot configuration precedence order (CLI > env > profile file > base file)",
      "Profile-specific file naming convention: application-{profile}.yml"
    ],
    "practical_judgment_candidates": [
      "Choosing @Value vs @ConfigurationProperties based on number of related settings",
      "Deciding how to manage secrets in production (env vars / Secret Manager vs hardcoded)"
    ],
    "error_interpretation_candidates": [
      "Application fails to start because @Value references a missing key with no default",
      "Wrong database connected in production due to missing profile activation"
    ],
    "scenario_candidates": [
      "Determining which datasource URL is active given CLI args and multiple YAML files",
      "Predicting Java object field values from a given YAML structure with @ConfigurationProperties"
    ],
    "common_wrong_answer_patterns": [
      "Believing base application.yml is always overridden entirely by profile files",
      "Thinking environment variables have lower priority than YAML files"
    ]
  }
}
