# CS Second-Pass Governance Batch 2

## Scope
- Migration: `00122_cs_second_pass_governance_seed_part2.sql`
- Focus categories:
  - `basic_theory`
  - `data_structure`
  - `algorithm`
  - `database`
  - `os`

## Purpose
- Close the remaining counted easy/medium shortages after `00121`.
- Keep the approach narrow:
  - relabel approved unset rows first
  - avoid new writing unless a recount still shows a real deficit

## Intended Closures
- `basic_theory`: `2 easy`, `1 medium`
- `data_structure`: `4 medium`
- `algorithm`: `3 medium`
- `database`: `2 easy`, `2 medium`
- `os`: `1 medium`

## Notes
- The `os` relabel in this batch is marked `adaptable` rather than `aligned`.
- If a later content review decides that row is too far from the lesson scope, replace it with a net-new OS medium question.
